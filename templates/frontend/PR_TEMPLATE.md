## 1) 复用依据（必填）
- 需求关键词：
- `/api-search` 检索范围（registry/openapi/src/api/pages）：
- 候选能力（>=3，含接口/页面复用理由；若不足 3 个请说明检索范围与结论）：
  - 候选 A：
  - 候选 B：
  - 候选 C：
- 最终选择（1个）与原因：
- 若未复用现有能力，给出理由：

## 2) 变更范围（必填）
- 业务目标（1-2 句）：
- 代码改动范围（views/components/api/router/store）：
- 是否新增或调整路由：是/否
- 是否变更权限守卫：是/否

## 3) 契约影响（必填）
- 受影响接口（逐条）：
- 契约结论：`有变更` / `无变更`
- 是否更新 `openapi.yaml`：是/否
- 若“无变更”，说明依据（参数/返回/错误码/鉴权）：
- 前后端兼容性评估：

## 4) 验证证据（必填）
- 本地验证命令：
  - ``
  - ``
- 页面验证清单（至少 2 条）：
  - 场景 1：
  - 场景 2：
- 关键截图/录屏说明（如适用）：

## 5) 风险与回滚（必填）
- 主要风险（交互、性能、权限、兼容性）：
- 监控与观察点（前端报错、接口失败率）：
- 回滚方案（步骤 + 触发条件）：

## 6) 提交前自检（勾选）
- [ ] 已执行复用检索，且记录候选能力 >= 3
- [ ] 改动落点符合目录规范，未散落实现
- [ ] 契约影响结论明确并附依据
- [ ] lint/tsc 与关键场景验证可复现
- [ ] 风险与回滚方案明确
